---
apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: lb-demo
  labels:
    app: client-service
spec:
  selector:
    app: client-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort
---
# Client in zone-a
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-service-zone-a
  namespace: lb-demo
  labels:
    app: client-service
    zone: zone-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-service
      zone: zone-a
  template:
    metadata:
      labels:
        app: client-service
        zone: zone-a
        topology.kubernetes.io/zone: zone-a
    spec:
      serviceAccountName: spring-cloud-kubernetes
      affinity:
        # Prefer to schedule pods in zone-a nodes
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values:
                      - zone-a
      containers:
        - name: client-service
          image: client-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: ZONE
              value: "zone-a"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 5
---
# Client in zone-b
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-service-zone-b
  namespace: lb-demo
  labels:
    app: client-service
    zone: zone-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-service
      zone: zone-b
  template:
    metadata:
      labels:
        app: client-service
        zone: zone-b
        topology.kubernetes.io/zone: zone-b
    spec:
      serviceAccountName: spring-cloud-kubernetes
      affinity:
        # Prefer to schedule pods in zone-b nodes
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values:
                      - zone-b
      containers:
        - name: client-service
          image: client-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: ZONE
              value: "zone-b"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 5

